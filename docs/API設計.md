# API 設計

| 項目 | 内容 |
| ---- | ---- |
| 文書名 | API 設計 |
| 関連 | [機能要件](./機能要件.md) / [データベース設計](./データベース設計.md) |
| 実装 | `trello-app/server/server.js` |
| 版数 | 1.0 |

---

## 1. 概要

| 項目 | 内容 |
| ---- | ---- |
| 方式 | REST（JSON） |
| ベース URL | `http://localhost:3000` |
| 認証 | なし（シングルユーザー） |

---

## 2. エンドポイント一覧

| メソッド | パス | 説明 | 関連 FR |
| -------- | ---- | ---- | ------- |
| GET | `/api/board` | ボード + リスト + カード一覧 | FR-021 |
| POST | `/api/lists/:listId/cards` | カード追加 | FR-010, FR-020 |
| DELETE | `/api/cards/:cardId` | カード削除 | FR-012, FR-020 |
| GET | `/api/health` | DB 接続確認 | 運用 |

---

## 3. GET /api/board

**レスポンス例**

```json
{
  "board": { "id": "1", "title": "マイタスクボード" },
  "lists": [
    {
      "id": "1",
      "title": "やること",
      "cards": [{ "id": "1", "title": "はじめてのタスク" }]
    }
  ]
}
```

---

## 4. POST /api/lists/:listId/cards

**リクエスト**

```json
{ "title": "タスク名" }
```

**レスポンス（201）**

```json
{ "id": "2", "title": "タスク名" }
```

**エラー**

| コード | 条件 |
| ------ | ---- |
| 400 | title が空 |
| 404 | listId が存在しない |

---

## 5. DELETE /api/cards/:cardId

**レスポンス** … 204 No Content

**エラー** … 404（カード不存在）

---

## 改訂履歴

| 版数 | 日付 | 改訂内容 |
| ---- | ---- | -------- |
| 1.0 | 2026/05/21 | 要件定義書から分離 |
